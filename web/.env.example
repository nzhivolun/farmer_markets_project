# .env.example — ОБРАЗЕЦ. Скопируйте в .env и подставьте значения.
# Этот файл коммитим, а .env — нет.

# Режим: 1 — включён (разработка), 0 — выкл. (приближенно к продакшен)
DEBUG=1

# Секретный ключ Django (замените на случайный длинный)
SECRET_KEY=replace_me_with_random_string

# Разрешённые хосты (через запятую). В разработке можно оставить звёздочку.
ALLOWED_HOSTS=*

# Параметры PostgreSQL
# ВАЖНО: если Postgres крутится в Docker и проброшен на хост 5432, то DB_HOST=localhost подойдёт.
# Если вы подключаетесь по имени сервиса из docker-compose, то DB_HOST=db
DB_NAME=farmer_markets
DB_USER=app_user
DB_PASSWORD=app_pass
DB_HOST=localhost
DB_PORT=5432
